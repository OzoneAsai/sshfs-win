diff --git a/sshfs.c b/sshfs.c
index 320ce21..f48fe8a 100644
--- a/sshfs.c
+++ b/sshfs.c
@@ -4550,6 +4550,8 @@ static void usage(const char *progname)
 "    -1                     equivalent to '-o ssh_protocol=1'\n"
 "    -o opt,[opt...]        mount options\n"
 "    -o reconnect           reconnect to server\n"
+"    -o forkless_spawn      on Cygwin, launch non-password SSH via Win32 CreateProcess\n"
+"    -o no_forkless_spawn   disable the Win32 forkless SSH launcher\n"
 "    -o stall_warn=N        report requests older than N seconds (no termination)\n"
 "    -o delay_connect       delay connection to server\n"
 "    -o sshfs_sync          synchronous writes\n"
