diff --git a/sshfs.c b/sshfs.c
index ff09439..7bbbb06 100644
--- a/sshfs.c
+++ b/sshfs.c
@@ -3928,10 +3928,20 @@ static int sshfs_opt_proc(void *data, const char *arg, int key,
 			 * Furthermore the mountpoint must NOT exist prior to mounting.
 			 * So we cannot use realpath(3).
 			 */
-			if ((('A' <= arg[0] && arg[0] <= 'Z') || ('a' <= arg[0] && arg[0] <= 'z'))
+			if ('\0' == arg[0] || ('*' == arg[0] && '\0' == arg[1])) {
+				/* WinFsp may supply an empty or wildcard mountpoint. */
+				sshfs.mountpoint = strdup(arg);
+			} else if ((('A' <= arg[0] && arg[0] <= 'Z') || ('a' <= arg[0] && arg[0] <= 'z'))
 				&& ':' == arg[1] && '\0' == arg[2]) {
 				/* drive: make a copy */
 				sshfs.mountpoint = strdup(arg);
+			} else if ('\\' == arg[0] && '\\' == arg[1] &&
+			    ('.' == arg[2] || '?' == arg[2]) && '\\' == arg[3] &&
+			    ((('A' <= arg[4] && arg[4] <= 'Z') ||
+			      ('a' <= arg[4] && arg[4] <= 'z'))) &&
+			    ':' == arg[5] && '\0' == arg[6]) {
+				/* Mount Manager form: \\.\X: or \\?\X: */
+				sshfs.mountpoint = strdup(arg);
 			} else {
 				/* path: split into dirname, basename and check dirname */
 				char *dir;
